<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.remi.ekio">

    <uses-permission android:name="android.permission.CAMERA_EXTENDED"></uses-permission>
    <uses-feature android:name="android.hardware.camera2" ></uses-feature>
    <uses-permission android:name="android.hardware.camera.autofocus"></uses-permission>
    <uses-permission android:name="android.permission.CAMERA" ></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" ></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>


    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BeforePictureActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".SaveInfoActivity"></activity>
        <activity android:name=".PhotoGrandEcranACtivity"></activity>
        <activity android:name=".CollectionShowcaseActivity"></activity>
        <meta-data  android:name="com.google.android.gms.version"
                    android:value="@integer/google_play_services_version" />

    </application>

</manifest>